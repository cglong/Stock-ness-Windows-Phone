<phone:PhoneApplicationPage
    x:Class="Stockness.View.StockPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
    DataContext="{Binding Stock, Source={StaticResource Locator}}">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="..\Images\ApplicationBackground2.jpg"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,30,0,0" RenderTransformOrigin="0.494,0.575"> 
            <Image Source="..\ApplicationIcon.png"
                    HorizontalAlignment="Left"
                    Height="70" Margin="12,-28,0,102" />
       
            <TextBlock Text="{Binding ApplicationTitle}" 
                               FontSize="75" 
                               Margin="87,-28,-2,102" />
        </Grid>


        <Grid Grid.Row="1"
              Margin="0,10,0,0">
            <StackPanel HorizontalAlignment="Left" Height="Auto" VerticalAlignment="Top" Width="Auto" RenderTransformOrigin="0.5,0.422">
                <TextBlock FontSize="40" RenderTransformOrigin="0.587,0.454" Height="45" Margin="0,0,-48,0">
            		<Run Text="{Binding StockSymbol}" Foreground="#818ba6"/>

                </TextBlock>
                <TextBlock FontSize="40" RenderTransformOrigin="0.587,0.454" Height="105" Margin="0,0,-48,0">
            		<Run Text="{Binding StockName}" Foreground="#818ba6" />

                </TextBlock>
                <TextBlock FontSize="40" RenderTransformOrigin="0.587,0.454" Height="45" Margin="0,0,-48,0">
            		<Run Text="Current: "/>
            		<Run Text="{Binding CurrPrice}"/>

                </TextBlock>
                <TextBlock FontSize="40" RenderTransformOrigin="0.587,0.454" Height="59" HorizontalAlignment="Left" Width="Auto" Margin="0,0,-83,0">
                	<Run Text="Change: "/>
                	<Run Text="{Binding PriceChange}" Foreground="#7fbf7f"/>
                	<Run Text=" (" Foreground="#7fbf7f"/>
                	<Run Text="{Binding PercentChange}" Foreground="#7fbf7f"/>
                	<Run Text=")" Foreground="#7fbf7f"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>